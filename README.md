# MIN — Realtime Messenger

MIN — небольшой учебный мессенджер с чатом в реальном времени на стекe **Node.js + Express + Socket.io (backend)** и **React + Vite + Socket.io-client (frontend)**.

Основной фокус проекта — на структуре репозитория, ведении задач, git flow, CI и документации, а не на сложной бизнес‑логике.

---

## 🚀 Функциональность

- Одна общая комната чата (один канал)
- Сообщения в реальном времени через WebSocket (Socket.io)
- Список онлайн‑пользователей с никнеймами
- Простая авторизация по нику (без регистрации и OAuth)
- История последних N сообщений в памяти
- Таймстемпы у сообщений

---

## 🛠 Стек технологий

### Backend

- Node.js
- Express.js
- Socket.io
- TypeScript

### Frontend

- React
- Vite
- Socket.io-client
- TypeScript
- CSS (Flexbox / Grid)

### Инфраструктура

- GitHub (репозиторий и PR)
- Git Flow / GitHub Flow
- CI на базе GitHub Actions (build)
- Трекер задач: Yandex Tracker

---

## 📂 Структура репозитория

MIN/  
├── .github/  
│ └── workflows/  
│ └── ci.yml # GitHub Actions CI (build проверка)  
│  
├── backend/ # Express.js + Socket.io сервер  
│ ├── src/  
│ │ ├── sockets/ # Socket.io обработчики событий  
│ │ ├── types/ # TypeScript интерфейсы (User, Message)  
│ │ └── server.ts # Главный файл сервера  
│ ├── dist/ # Скомпилированный JavaScript  
│ ├── package.json  
│ ├── tsconfig.json  
│ └── node_modules/ # (в .gitignore)  
│  
├── frontend/ # React + Vite чат-интерфейс  
│ ├── src/  
│ │ ├── components/ # React компоненты (LoginForm, ChatWindow, UserList)  
│ │ ├── hooks/ # Custom React hooks (useSocket)  
│ │ ├── types/ # TypeScript интерфейсы  
│ │ ├── App.tsx # Главный компонент  
│ │ ├── main.tsx # Entry point  
│ │ └── App.css  
│ ├── dist/ # Собранный фронтенд  
│ ├── public/ # Статические файлы  
│ ├── index.html  
│ ├── package.json  
│ ├── vite.config.ts  
│ ├── tsconfig.json  
│ └── node_modules/ # (в .gitignore)  
│  
├── docs/ # Документация  
├── .gitignore # node_modules, dist и т.д.  
├── CONTRIBUTING.md  
├── LICENSE  
└── README.md

---
## ⚙️ Требования

- Node.js: LTS (например, 18+)
- npm / pnpm / yarn
- Git
---
## 🔧 Установка и запуск (локально)

### 1. Клонировать репозиторий
```
git clone https://github.com/<your-org>/MIN.git
cd min
```
### 2. Backend
```
cd backend
npm install
npm run dev
```
Backend по умолчанию: `http://localhost:3001`.

### 3. Frontend
```
cd ../frontend
npm install
npm run dev
```
Фронтенд по умолчанию: `http://localhost:5173`.

---

## 🔄 Git Flow / ветки

- `main` — стабильная ветка, только через Pull Request
- `develop` — основная ветка разработки (опционально)
- `feature/*` — фичи

---

## 👥 Роли в команде

- **Dev #1 (Backend)** — структура репозитория, сервер, WebSocket, CI.
- **Dev #2 (Frontend)** — UI, интеграция с backend, документация, README.
