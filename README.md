# MIN — Realtime Messenger

MIN — небольшой учебный мессенджер с чатом в реальном времени на стекe **Node.js + Express + Socket.io (backend)** и **React + Vite + Socket.io-client (frontend)**.

Основной фокус проекта — на структуре репозитория, ведении задач, git flow, CI и документации, а не на сложной бизнес‑логике.

---

## 🚀 Функциональность

- Одна общая комната чата (один канал)
- Сообщения в реальном времени через WebSocket (Socket.io)
- Список онлайн‑пользователей с никнеймами
- Простая авторизация по нику (без регистрации и OAuth)
- История последних N сообщений в памяти
- Уведомления о входе/выходе пользователей
- Таймстемпы у сообщений

---

## 🛠 Стек технологий

### Backend

- Node.js
- Express.js
- Socket.io
- TypeScript
- ESLint

### Frontend

- React
- Vite
- Socket.io-client
- TypeScript
- CSS (Flexbox / Grid)

### Инфраструктура

- GitHub (репозиторий и PR)
- Git Flow / GitHub Flow
- CI на базе GitHub Actions (build + lint)
- Трекер задач: Yandex Tracker / Jira / Trello / YouTrack (на выбор)

---

## 📂 Структура репозитория
MIN/
├── backend/ # Backend: Node.js + Express + Socket.io
│ ├── src/
│ │ ├── server.ts # Точка входа сервера
│ │ ├── sockets/
│ │ │ ├── handlers/
│ │ │ │ ├── messageHandler.ts
│ │ │ │ ├── userHandler.ts
│ │ │ │ └── roomHandler.ts (опционально)
│ │ │ └── events.ts # Константы событий Socket.io
│ │ ├── services/ # Бизнес‑логика
│ │ ├── models/ # Типы / модели
│ │ ├── middleware/ # Middleware
│ │ ├── config/ # Конфиги, константы
│ │ └── types/ # Общие типы TypeScript
│ ├── .eslintrc.json
│ ├── tsconfig.json
│ ├── package.json
│ └── README.md
│
├── frontend/ # Frontend: React + Vite
│ ├── src/
│ │ ├── App.tsx
│ │ ├── main.tsx
│ │ ├── components/
│ │ │ ├── Chat/
│ │ │ └── Auth/
│ │ ├── hooks/
│ │ ├── types/
│ │ └── styles/
│ ├── vite.config.ts
│ ├── tsconfig.json
│ ├── .eslintrc.json
│ ├── package.json
│ └── README.md
│
├── docs/ # Документация
│ ├── ARCHITECTURE.md
│ ├── API_EVENTS.md
│ └── SETUP.md
│
├── .github/
│ └── workflows/
│ └── ci.yml # CI: build + lint
│
├── .gitignore
├── README.md # Этот файл
└── CONTRIBUTING.md # Правила участия
---
## ⚙️ Требования

- Node.js: LTS (например, 18+)
- npm / pnpm / yarn
- Git
---
## 🔧 Установка и запуск (локально)

### 1. Клонировать репозиторий
git clone https://github.com/<your-org>/MIN.git
cd min

### 2. Backend
cd backend
npm install
cp .env.example .env
npm run dev # или npm start — по вашим скриптам
Backend по умолчанию: `http://localhost:3001`.

### 3. Frontend
cd ../frontend
npm install
npm run dev
Фронтенд по умолчанию: `http://localhost:5173`.

---

## 🔄 Git Flow / ветки

- `main` — стабильная ветка, только через Pull Request
- `develop` — основная ветка разработки (опционально)
- `feature/*` — фичи
- `hotfix/*` — срочные фиксы

---

## 👥 Роли в команде

- **Dev #1 (Backend)** — структура репозитория, сервер, WebSocket, CI.
- **Dev #2 (Frontend)** — UI, интеграция с backend, документация, README.
